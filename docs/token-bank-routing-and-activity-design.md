# Token 银行：分组接收与收益动态建议

2026-09-25。基于当前代码与线上只读核查的设计建议，尚未实施；本次不修改线上配置。延续 token-bank-design.md 的账号、渠道和独立分账复用方案。

## 分组由管理员配置，C 端只授权账号

C 端不提供分组选择。添加页显示平台、账号授权方式，以及只读的接收分组、分成比例；后台按平台匹配管理员已经配置的接收分组。已有账号创建、授权、导入、编辑服务继续复用。

当前逻辑：渠道 features_config.token_savings.receiving_group_ids → 筛选启用且平台相同的分组 → 创建原 accounts 记录并绑定当前用户 → 写入原账号与分组关系。一旦配置多个同平台接收分组，新账号会加入全部匹配组。C 端创建时传入的 group_ids 会被覆盖，编辑时不能改分组。

配置变更影响之后的添加；已有账号不会因此自动迁移，应由管理员在原账号管理中批量调整。一个账号可以绑定多个分组，按实际请求所属分组、渠道计费及分账。

## 线上当前映射

| 平台或入口 | 实际分组 | 当前情况 |
| --- | --- | --- |
| DeepSeek 普通调用 | deepseek，ID 15 | 已启用储蓄接收，用户 80%，管理员 20% |
| DeepSeek 月卡 | 焚诀·启笔·月卡，ID 12 | DeepSeek 平台，与 ID 15 同属渠道 3，可加入接收配置 |
| GPT | GPT Pro ×20，ID 19 | 对外倍率 0.8，尚未绑定渠道、启用储蓄接收 |
| Claude / Gemini | Claude Pro / Gemini Pro，ID 20 / 21 | 尚未绑定渠道、启用储蓄接收 |
| 混合月卡 | 焚诀·成章·月卡 / 焚诀·著作·月卡，ID 22 / 23 | composite 平台；已有模型到平台路由及多平台账号，但当前储蓄接收校验不允许 composite |

建议第一步只配置 DeepSeek 普通组与启笔月卡这两个已经匹配的平台组，无需新增账号体系或分组选择器。混合月卡保留原模型路由；若后续开放自动接收，必须在原渠道接收配置中明确平台到混合组的对应关系，不能直接取消平台检查。

没有配置对应接收组的平台应在添加页就显示“暂未开放，等待平台配置”，不让用户完成授权后才遇到保存失败；不自动落到默认组或其他平台组。

平台匹配与成本准入分别处理。平台相同不等于账号成本相同，也不证明填入的凭据有效；应复用原授权与连接测试验证，成本、倍率和利润门由管理员维护。GPT 的 0.8 售价、30% 毛利口径仍待用户确认，不能把原成本倍率过滤当作储蓄分账后的净利润保证。

## 月卡分账口径

当前结算基数为 BalanceCost + SubscriptionCost。月卡调用按额度消耗金额参与分账，并非按月卡购买款实时摊分。月卡售价、赠送额度及使用率会影响平台实际收益；绑定月卡前需确认接受该既有口径，不在本方案内另建摊销或结算系统。

## 收益展示：先做可折叠的储蓄动态

建议 Token 银行顶部收益卡片下增加一块默认收起的“储蓄动态”，由用户展开；桌面和手机均放在页面内，不用浮动弹窗。排行榜不是添加账号的前置需求，可在真实使用量足够后再做。

折叠态：储蓄动态 · 查看近期已到账收益。

展开态：最近到账记录，最多 10 条；展示用户主动允许公开的匿名名称、平台、到账金额和相对时间。样式示意“储蓄用户 A*** · DeepSeek · 已到账 ¥/US$ 金额 · 5 分钟前”；币种与原账本一致，不自行换汇。无公开记录则显示空态，不造数据。

只有展开且页面可见时每 60 秒刷新一次；时间到不强制播报、不重复播放旧记录。读取已经成功提交的收益账本，不另算收益、不改计费、不按调用总额冒充到账金额。

公开属于新增的数据用途，应允许用户选择是否参与，默认不公开个人收益。简单遮挡昵称并不能替代公开选择；不暴露邮箱、账号标识、凭据、余额或消费方资料。未参与用户仍可正常储蓄并查看自己的收益。

后续可加最近 7 日收益榜，按同一用户多个账号的已到账收益汇总，明确时区、统计范围与更新时间。首版先不加入每日弹窗、全站滚动播报、排行榜奖励或收益承诺。

## 验收边界

- DeepSeek 新账号只能落到已配置的 DeepSeek 接收组；伪造 GPT/其他 group_ids 不能改路由。
- 未开放平台在授权前提示；原管理员账号管理仍能按已有权限配置多分组。
- 原有账号迁移明确列出受影响账号，不把接收配置保存当作迁移完成。
- 月卡按既有额度口径分账，重复请求不重复入账；成本和售价不因收益展示变化。
- 动态只取真实成功账本，未参与公开的用户不可出现在接口结果中；收起后停止刷新。
